### Question=1
Which version of the FTP server is running on the target system? Submit the entire banner as the answer.

### Answer

I tried to run nmap -sV -sC -A 192.168.x.x  but doesnt show the verssion of the ftp but showing that ftp is running on port 21 , i had run -sC also so it gave me that anonymous login is allowed .

To find the vesion of the ftp we have to connect the server using nc 10.129.35.215 21 it will show the banner of ftv service We also have one more command of nmap that is nmap -p21 -sV --script=banner 10.129.35.215

### Question.2
Enumerate the FTP server and find the flag.txt file. Submit the contents of it as the answer.

### Answer

We know that anonymous login is allowed we will login get the flag 
